# .env.sample
# 이 파일은 애플리케이션에 필요한 환경 변수 예시입니다.
# 이 파일을 .env 파일로 복사한 후 실제 값으로 채워주세요.

# --- Ollama 설정 ---
# 사용할 Ollama 모델을 지정합니다. (예: qwen2:7b, llama3:8b)
OLLAMA_MODEL_NAME=qwen3:1.7b

# Ollama 서버의 기본 URL (포트 포함)
# (예: http://localhost:11434 또는 Docker 환경의 경우 http://ollama:11434)
OLLAMA_BASE_URL=http://ollama:11434

# --- MCP 서버 설정 ---
# 실행 중인 fastapi-mcp 서버 엔드포인트의 전체 URL
# (예: http://localhost:8011/mcp 또는 Docker 환경의 경우 http://host.docker.internal:8011/mcp)
MCP_SERVER_URL=http://host.docker.internal:8011/mcp